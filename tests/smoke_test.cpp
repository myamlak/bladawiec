// Placeholder test proving the GoogleTest harness is wired end-to-end.
// Replace with real module tests as Epic 2 code lands.
#include <gtest/gtest.h>

TEST(SmokeTest, BasicAssertions) {
    EXPECT_EQ(1 + 1, 2);
}
